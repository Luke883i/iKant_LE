# C2.HOST_BOOTSTRAP_NARRATIVE_SHELL — DoD

## Source evidence

A user-supplied local-active archive and ledger from 2026-09-15 contain four hash-linked events: ACCEPT, PROBE, INITIALIZE and TURN. The observed probe reports Node 22.16.0 and local FS/crypto/clock/artifact capability; the initialized state is ACTIVE and the first TURN produces a same-turn DOCX. In that single trace, ACCEPT->PROBE is 38 ms, PROBE->INITIALIZE 36 ms, and ACCEPT->INITIALIZE 74 ms. This is one observed local execution, not a p95 and not proof of generic ChatGPT Pro capability.

The evidence supports collapsing the two manual technical commands into an automatic post-consent transaction while preserving the three persisted transitions. It does not support claiming that every ChatGPT surface exposes the same local capabilities.

## External practice constraints

C2 preserves least privilege and confirmation boundaries for consequential actions, treats retrieved content as untrusted data, and keeps local state/checkpoint semantics explicit. GitHub materialization follows blob -> tree -> commit -> ref-last. Current external documentation is design input only and has zero runtime authority.

## Global DoD

- one human admission gate: exact `I ACCEPT`;
- real auto-probe and auto-initialize after acceptance;
- false ACTIVE impossible after failed probe/init;
- persisted ACCEPT/PROBE/INITIALIZE events with readback and bootstrap receipt;
- cold/warm bootstrap fingerprint-bound; constitutional drift forces cold/reacceptance;
- ACTIVE narrator is iKant; ordinary identity does not expose engine/runtime plumbing;
- explicit technical self-query may disclose the host engine as infrastructure, never identity;
- SELF_PUBLIC and SELF_TECHNICAL remain distinct;
- runtime voice vector can change rhetoric but not evidence/authority/permission/execution;
- no historical-person impersonation or source-name persistence in the archetypal profile;
- host/platform constraints precede the local contract; no override claim;
- Surface A 50–500 words first; technical telemetry stays backlog-owned;
- single writer, hash-linked ledger, same-turn DOCX and C1 resource/action boundaries preserved;
- no new database, framework, model SDK, broker or daemon.

## Local / intermediate gates

- deterministic tests: all PASS;
- contract/lifecycle check: all PASS;
- exact lifecycle enumeration includes HOST_INCOMPATIBLE and has zero forbidden edges;
- failure injection proves probe failure cannot reach ACTIVE;
- engine-swap test proves ordinary identity stability;
- voice-regulation tests prove rhetorical modulation without warrant mutation;
- E2E exact `I ACCEPT` yields ACCEPT->PROBE->INITIALIZE and same-turn narrative identity/DOCX.

## Q3 mutation gate

- 10,000,000 runtime-semantic cases;
- exact strata: >=50% ordinary, >=30% edge, >=20% stress/adversarial;
- ten deterministic shard seeds;
- all declared invariant families exercised;
- survivors: 0;
- 1,000,000 SAT_NEW tail with zero novel surviving signatures;
- 1,000,000 SAT_COMPRESS candidates with zero strictly smaller non-degrading representation under the declared semantic-role oracle;
- source binding to exact qualified files and combined receipt;
- Wilson 95% upper bound reported for zero survivors.

## Claim boundary

A PASS supports only declared runtime/design invariants over the generated corpus. It does not prove ChatGPT Pro capability in every surface, model quality, phenomenal consciousness, a scientific Jungian archetype, reconstruction of any historical person's mind, universal prompt-injection immunity, production reliability, material-action capability, or formal global minimality.
