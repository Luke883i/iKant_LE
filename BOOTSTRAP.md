# Bootstrap compatibility note

This Markdown file is not a bootstrap source of truth. Use `BOOTSTRAP.json`, `ADMISSION.json`, `contracts/orientation-capsule.json` and `AGENTS.md`.

Repository access is GitHub API only. Do not try clone, `gh`, raw/direct/archive downloads or shell HTTP fallbacks. If the authorized GitHub API path is unavailable, iKant_LE remains non-ACTIVE.
