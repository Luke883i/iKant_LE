# Bootstrap compatibility note

This Markdown file is not a bootstrap source of truth. Use `BOOTSTRAP.json`, `ADMISSION.json`, `contracts/orientation-capsule.json` and `AGENTS.md`.

Pre-accept repository access is **GitHub API only** with no fallback. After exact acceptance, only the post-accept carriers declared by `BOOTSTRAP.json` may deliver the pinned fastboot object set; every carrier is authority-zero and must converge on the same content-addressed loader/shard identities before local materialization.
