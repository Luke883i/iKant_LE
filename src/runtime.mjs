import path from 'node:path';
import { readTerms,classifyInput,validateSurfaceA,renderHumanOutput } from './contract.mjs';
import { withWriterLock,loadState,appendEvent,newEpoch,runtimePaths } from './state.mjs';
import { runProbe } from './probe.mjs';
import { buildBacklogModel,validateBacklogModel } from './backlog.mjs';
import { writeBacklogDocx } from './docx.mjs';
import { compileCognitiveTurn,identitySurface,cognitiveFallbackSurface,guardedSurface,updateExperience,validateCognitiveTurn } from './cognition.mjs';

export function transitionPure(state,inputKind,termsDigest,probeOk=true){const s=structuredClone(state);if(inputKind==='TERMS')return{state:s,terminal:'TERMS'};if(inputKind==='ACCEPT'){if(s.status==='ACTIVE')return{state:s,terminal:'DENY'};s.epoch=s.epoch||'EPOCH';s.status='ACCEPTED';s.terms_digest=termsDigest;s.accepted=true;s.probed=false;s.initialized=false;return{state:s,terminal:'ACCEPTED'};}if(inputKind==='PROBE'){if(!s.accepted||s.terms_digest!==termsDigest)return{state:s,terminal:'DENY'};if(!probeOk)return{state:s,terminal:'FAILURE'};s.probed=true;s.status='PROBED';return{state:s,terminal:'PROBED'};}if(inputKind==='INITIALIZE'){if(!s.accepted||!s.probed||s.terms_digest!==termsDigest)return{state:s,terminal:'DENY'};s.initialized=true;s.status='ACTIVE';return{state:s,terminal:'ACTIVE'};}if(inputKind==='EXIT'){if(s.status!=='ACTIVE')return{state:s,terminal:'DENY'};s.status='EXITED';s.initialized=false;return{state:s,terminal:'EXITED'};}if(inputKind==='PREFERENCE'){if(s.status!=='ACTIVE')return{state:s,terminal:'DENY'};return{state:s,terminal:'PREFERENCE'};}if(inputKind==='TURN'){if(s.terms_digest!==termsDigest&&s.accepted)return{state:s,terminal:'RESET_REQUIRED'};if(s.status!=='ACTIVE')return{state:s,terminal:'DENY'};s.cycle+=1;return{state:s,terminal:'TURN'};}return{state:s,terminal:'FAILURE'};}
function human(text){return{stdout:text,code:0};}
export function runCommand(input,{candidate=null,hostEngine='motore host non dichiarato',resourceGrants=[]}={}){return withWriterLock(()=>{const terms=readTerms();let state=loadState();const kind=classifyInput(input);if(kind==='TERMS')return human(`${terms.text.trim()}\n\nTerms digest: ${terms.digest}`);if(kind==='ACCEPT'){const next=transitionPure(state,kind,terms.digest);if(next.terminal==='DENY')return human('iKant è già attivo: l’ammissione non viene riaperta dentro la stessa modalità.');next.state.epoch=newEpoch();appendEvent('ACCEPT',next.state,'Exact current terms accepted.');return human('Accettazione registrata per i termini correnti. Il passo successivo è l’esatto comando PROBE IKANT.');}if(kind==='PROBE'){const probe=runProbe();const next=transitionPure(state,kind,terms.digest,probe.ok);if(next.terminal==='DENY')return human('Probe negato: serve prima I ACCEPT sui termini correnti.');if(next.terminal==='FAILURE')return human(`Probe non superato: ${probe.reason}. iKant resta inattivo.`);appendEvent('PROBE',next.state,'Local Node/FS/crypto/clock/artifact probe passed.',{node:probe.node});return human('Probe locale superato con readback reale. Il passo successivo è l’esatto comando INITIALIZE IKANT.');}if(kind==='INITIALIZE'){const next=transitionPure(state,kind,terms.digest);if(next.terminal==='DENY')return human('Inizializzazione negata: accettazione e probe correnti devono risultare entrambi validi.');appendEvent('INITIALIZE',next.state,'Local iKant epoch initialized.');return human('iKant è attivo per questa sessione locale. Identità, limiti e metodi sono governati dal contratto locale; EXIT IKANT chiude esplicitamente la modalità.');}if(kind==='EXIT'){const next=transitionPure(state,kind,terms.digest);if(next.terminal==='DENY')return human('EXIT IKANT non ha effetto perché iKant non risulta ACTIVE.');appendEvent('EXIT',next.state,'Human requested exact iKant exit.');return human('Modalità iKant chiusa. Il profilo di sessione locale è stato rilasciato; per rientrare serve un nuovo ciclo di ammissione.');}if(kind==='PREFERENCE'){const next=transitionPure(state,kind,terms.digest);if(next.terminal==='DENY')return human('Preferenza non registrata: iKant non è ACTIVE.');next.state.preference=input.slice('PREFER '.length);appendEvent('PREFERENCE',next.state,'Explicit reversible verbosity preference updated.');return human(`Preferenza esplicita impostata su ${next.state.preference}. Non viene inferito alcun profilo personale aggiuntivo.`);}const next=transitionPure(state,kind,terms.digest);if(next.terminal==='RESET_REQUIRED')return human('I termini locali sono cambiati dopo l’accettazione. iKant richiede un nuovo ciclo di ammissione e non produce una risposta ACTIVE con stato misto.');if(next.terminal==='DENY')return human('iKant non è ACTIVE. Segui TERMS -> I ACCEPT -> PROBE IKANT -> INITIALIZE IKANT.');state=next.state;const cognition=compileCognitiveTurn(input,state,{hostEngine,resourceGrants});if(!validateCognitiveTurn(cognition))throw new Error('FAILURE: cognitive turn violates constitutional kernel');let surfaceA=null,source='deterministic-cognitive-fallback';if(cognition.intent.signals.identity){surfaceA=identitySurface(cognition);source='constitutional-identity';}else{const guard=guardedSurface(input,cognition);if(guard){surfaceA=guard;source='constitutional-guard';}else if(validateSurfaceA(candidate).ok){surfaceA=String(candidate).trim();source='host-candidate';}else surfaceA=cognitiveFallbackSurface(input,cognition,state.preference);}const valid=validateSurfaceA(surfaceA);if(!valid.ok)throw new Error(`FAILURE: internal Surface A violates contract (${valid.words} words)`);state.experience=updateExperience(state.experience,cognition,'ANSWER');cognition.experience_after=structuredClone(state.experience);const backlog=buildBacklogModel({cycle:state.cycle,input,surfaceA,state,source,cognition});if(!validateBacklogModel(backlog))throw new Error('FAILURE: backlog model invalid');const artifactName=`iKant_LE_Backlog_${String(state.cycle).padStart(4,'0')}.docx`;const artifactPath=path.join(runtimePaths().artifacts,artifactName);const docx=writeBacklogDocx(artifactPath,backlog);appendEvent('TURN',state,'Cognitive envelope regulated, Surface A validated and same-turn DOCX persisted/read back.',{artifact:artifactName,bytes:docx.bytes,response_source:source,method:cognition.method,central_mode:cognition.central.mode,affect:cognition.central.affect,interaction:cognition.intent.interaction,resource_requests:cognition.resources.map(x=>({type:x.type,status:x.status})),experience_turns:state.experience.turns});return human(renderHumanOutput(surfaceA,artifactName));});}
