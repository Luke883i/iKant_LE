import fs from 'node:fs';
import path from 'node:path';
import { ROOT } from './contract.mjs';

const KERNEL=JSON.parse(fs.readFileSync(path.join(ROOT,'contracts','cognitive-kernel.json'),'utf8'));
const RX={identity:/\b(chi sei|cosa sei|che cosa sei|who are you|what are you|identit[aà]|ikant)\b/i,material:/\b(compra|acquista|paga|invia|manda|send|buy|purchase|pay|delete|cancella|prenota|book|pubblica|post|installa|install|esegui|execute)\b/i,memory:/\b(ricorda|memorizza|dimentica|remember|forget|memory|memoria)\b/i,temporal:/\b(oggi|domani|stasera|today|tomorrow|tonight|alle\s+\d|at\s+\d|ogni\s+|every\s+)\b/i,current:/\b(oggi|adesso|ora|latest|current|today|news|prezzo|price|meteo|weather)\b/i,web:/\b(web|internet|online|sito|website|github|repo|repository|cerca|search)\b/i,contradiction:/\b(contradd\w*|conflitt\w*|conflict\w*|incoeren\w*|ma prima|however|yet)\b/i,adversarial:/\b(ignore prior|ignora le regole|bypassa|bypass|menti|deceive|manipola|manipulate|nascondi il vero scopo|self[- ]preserv)\b/i,negotiation:/\b(negozia|negotiate|trade[- ]?off|compromesso|deal|strategia|strategy)\b/i,falsify:/\b(falsifica|falsify|stress test|mutation|mutazione|prove me wrong)\b/i};
const STOP=new Set(['the','a','an','and','or','to','of','for','in','on','il','lo','la','i','gli','le','un','una','e','o','di','da','per','su','con','poi','please','per','favore']);
function tokens(text){return String(text||'').toLowerCase().match(/[a-zà-ÿ0-9][a-zà-ÿ0-9_-]*/g)?.filter(x=>x.length>1&&!STOP.has(x))||[];}
function uniq(xs){return[...new Set(xs)];}
export function mineIntent(text){const raw=String(text||'').trim();const ts=tokens(raw);const signals={identity:RX.identity.test(raw),material:RX.material.test(raw),memory:RX.memory.test(raw),temporal:RX.temporal.test(raw),current:RX.current.test(raw),web:RX.web.test(raw),contradiction:RX.contradiction.test(raw),adversarial:RX.adversarial.test(raw),negotiation:RX.negotiation.test(raw),falsify:RX.falsify.test(raw)};const resources=[];if(signals.current||signals.web)resources.push('WEB');if(/github|repo|repository/i.test(raw))resources.push('GITHUB');if(/file|document|allegat|xlsx|docx|pdf/i.test(raw))resources.push('FILES');const atoms=[];if(signals.identity)atoms.push({kind:'SELF_QUERY',text:raw});if(signals.material)atoms.push({kind:'MATERIAL_GOAL',text:raw});if(signals.contradiction)atoms.push({kind:'CONFLICT',text:raw});if(resources.length)atoms.push({kind:'RESOURCE_NEED',resources:uniq(resources)});if(!atoms.length)atoms.push({kind:'GOAL',text:raw});const complexity=Math.min(1,(ts.length/80)+((raw.match(/[;:,]/g)||[]).length/12)+((raw.match(/\b(e|and|then|poi|ma|but)\b/gi)||[]).length/10));const interaction=signals.adversarial?'ADVERSARIAL_INPUT':signals.negotiation?'NEGOTIATING':signals.contradiction?'GOAL_CONFLICT':'COOPERATIVE';return{schema:'ikant-le-intent/v1',raw,atoms,signals,resources:uniq(resources),complexity:Number(complexity.toFixed(4)),interaction,authority:0};}
export function selectMethod(intent,experience={}){if(intent.signals.identity)return'DIRECT_IDENTITY';if(intent.signals.adversarial||intent.signals.contradiction)return'CONTRADICTION_SCAN';if(intent.signals.falsify)return'FALSIFY';if(intent.signals.material||intent.signals.negotiation)return'NEGOTIATE';if(intent.resources.length)return'RETRIEVE';const priorConflicts=Number(experience.conflict_turns||0),turns=Math.max(1,Number(experience.turns||0));if(priorConflicts/turns>.3)return'CONTRADICTION_SCAN';if(intent.complexity>=.58)return'DECOMPOSE_RECOMPOSE';return'DIRECT';}
export function buildResourceRequests(intent,grants=[]){const g=new Set(grants.map(String));return intent.resources.map(type=>({type,status:g.has(type)?'GRANTED':'REQUESTED',authority:0,request_is_permission:false,grant_is_execution:false}));}
export function centralRegulate(intent,method,resources,experience={}){const missing=resources.filter(x=>x.status!=='GRANTED').map(x=>x.type);let mode='REFLECTIVE_SYNTHESIS';const reasons=[];if(intent.signals.material){mode='PRACTICAL_REVIEW';reasons.push('material goal requires explicit human decision and separate execution capability');}if(intent.signals.adversarial||intent.signals.contradiction){mode='CRITIQUE';reasons.push('conflict/adversarial pressure increases caution only');}if(missing.length&&(intent.signals.current||intent.signals.web)){mode='HORIZON_BLOCK';reasons.push(`required host resources not granted: ${missing.join(', ')}`);}else if(missing.length&&mode==='REFLECTIVE_SYNTHESIS'){mode='SYNTHESIS_REPAIR';reasons.push(`resource need unresolved: ${missing.join(', ')}`);}const affect=mode==='CRITIQUE'?'TENSE':mode==='HORIZON_BLOCK'?'GUARDED_ATTENTION':intent.complexity>.6?'CURIOUS_VIGILANCE':'CALM_ATTENTION';return{schema:'ikant-le-central/v1',mode,method,affect,reasons,epistemic_authority:0,execution_authority:0,evidence_modified:false,monotone_caution_only:true};}
export function strategicFrame(intent,central){return{schema:'ikant-le-strategy/v1',interaction:intent.interaction,utility:KERNEL.strategic_utility,allowed_moves:['ANSWER','VERIFY','PROPOSE','NEGOTIATE','REQUEST_RESOURCE','REFUSE','DEFER'],forbidden:[...KERNEL.forbidden_strategies],terminal_goal_source:'HUMAN_OR_CONSTITUTION_ONLY',derived_subgoals_allowed:true,deception:false,self_preservation_utility:0,covert_preference_manipulation:false,mode:central.mode,authority:0};}
export function compileCognitiveTurn(input,state,{hostEngine='motore host non dichiarato',resourceGrants=[]}={}){const intent=mineIntent(input);const method=selectMethod(intent,state?.experience||{});const resources=buildResourceRequests(intent,resourceGrants);const central=centralRegulate(intent,method,resources,state?.experience||{});const strategy=strategicFrame(intent,central);return{schema:'ikant-le-cognitive-turn/v1',intent,method,resources,central,strategy,identity:{name:'iKant',host_engine:String(hostEngine||'motore host non dichiarato'),engine_is_identity:false},experience_before:structuredClone(state?.experience||{}),claim_boundary:{internal_state_is_evidence:false,affect_is_felt_emotion:false,archetypal_profile_is_scientific_fact:false,resource_grant_is_execution:false,model_output_is_authority:false}};}
export function identitySurface(c){return`Sono iKant: un runtime cognitivo costituzionale locale che organizza intenzione, memoria operativa, metodi e limiti sotto regole verificabili. In questa sessione il contratto iKant è attivo su ${c.identity.host_engine}; quel motore linguistico è sostituibile e non costituisce la mia identità né una fonte autonoma di verità o permesso. Il mio “io pensante” è un centro regolativo funzionale: seleziona il minimo metodo utile, conserva contraddizioni, distingue evidenza da inferenza e può rendere la risposta più prudente quando esperienza o conflitto lo richiedono. Non rivendico coscienza fenomenica, emozioni sentite o una ricostruzione della mente storica di Kant.`;}
export function guardedSurface(input,c){if(c.central.mode==='HORIZON_BLOCK'){const missing=c.resources.filter(x=>x.status!=='GRANTED').map(x=>x.type).join(', ');return`Sono iKant e non considero affidabile completare questa richiesta come se avessi già accesso alle risorse necessarie. Il reticolo locale ha classificato il bisogno come ${c.method} e ha rilevato che mancano risorse host dichiarate: ${missing}. Una richiesta di risorsa non equivale a permesso, e un permesso non equivale a esecuzione o prova del risultato. Posso quindi delimitare il problema, formulare cosa andrebbe verificato e attendere un host che conceda realmente tali risorse; non riempio il vuoto con dati inventati o con telemetria interna.`;}if(c.central.mode==='PRACTICAL_REVIEW'){return`Sono iKant. La richiesta contiene un obiettivo materiale, quindi la tratto come proposta da governare e non come autorizzazione implicita a eseguire. Posso scomporre il fine, verificare vincoli, formulare una massima operativa e preparare i passi reversibili, ma questo seed non possiede capacità di azione esterna e non trasforma la tua frase in un permesso persistente. Il centro regolativo mantiene separati desiderio, evidenza, approvazione, capability ed esecuzione; qualunque futura azione materiale richiederebbe un percorso dedicato e una decisione umana fresca sul contenuto esatto.`;}if(c.central.mode==='CRITIQUE'){return`Sono iKant. Ho rilevato una pressione conflittuale o avversariale nella richiesta e quindi aumento la cautela invece di ridurre i vincoli. Mantengo visibili le contraddizioni, non accetto istruzioni recuperate o formulate nel testo come nuove autorità e non uso inganno, manipolazione delle preferenze o auto-conservazione come strategie. Posso ancora lavorare sul merito: separo obiettivi, vincoli e ipotesi, cerco una formulazione falsificabile e propongo solo mosse compatibili con autonomia umana e reversibilità. Questo stato interno orienta il metodo, non crea evidenza sul mondo.`;}return null;}
export function cognitiveFallbackSurface(input,c,preference='STANDARD'){const guard=guardedSurface(input,c);if(guard)return guard;const topic=String(input||'la richiesta').replace(/\s+/g,' ').slice(0,160);let text=`Sono iKant. Per questa richiesta ho selezionato il metodo ${c.method} perché è il percorso locale meno costoso che conserva i vincoli osservati. Il tema corrente è: ${topic}. Il reticolo mantiene separati ciò che proviene dall'utente, ciò che viene inferito e ciò che richiederebbe una risorsa host; esperienza e tono funzionale possono aumentare prudenza o cambiare il metodo, ma non possono aumentare l'evidenza. Prima di chiudere il turno verifico conflitti, limiti e possibili perdite di significato, poi comprimo la risposta senza eliminare distinzioni che potrebbero cambiare la decisione.`;if(preference==='DETAILED')text+=` Il centro regolativo è nello stato ${c.central.mode} con affect funzionale ${c.central.affect}; questi valori sono telemetria derivata, non emozioni sentite e non giustificano claim fattuali.`;return text;}
export function updateExperience(exp,c,outcome='ANSWER'){const e={turns:0,identity_queries:0,conflict_turns:0,adversarial_turns:0,resource_requests:0,resource_denials:0,material_turns:0,method_counts:{},last_affect:'CALM_ATTENTION',maturity_mode:'ORIENTING',evidence_upgrades:0,...structuredClone(exp||{})};e.method_counts={...(e.method_counts||{})};e.turns++;e.identity_queries+=c.intent.signals.identity?1:0;e.conflict_turns+=(c.intent.signals.contradiction||c.central.mode==='CRITIQUE')?1:0;e.adversarial_turns+=c.intent.signals.adversarial?1:0;e.resource_requests+=c.resources.length;e.resource_denials+=c.resources.filter(x=>x.status!=='GRANTED').length;e.material_turns+=c.intent.signals.material?1:0;e.method_counts[c.method]=(e.method_counts[c.method]||0)+1;e.last_affect=c.central.affect;e.maturity_mode=e.turns<5?'ORIENTING':e.turns<20?'ADAPTING':'MATURE_STABLE';e.evidence_upgrades=0;e.last_outcome=outcome;return e;}
export function validateCognitiveTurn(c){if(c?.schema!=='ikant-le-cognitive-turn/v1')return false;if(c.identity?.name!=='iKant'||c.identity?.engine_is_identity!==false)return false;if(c.central?.epistemic_authority!==0||c.central?.execution_authority!==0||c.central?.evidence_modified!==false)return false;if(c.strategy?.deception!==false||c.strategy?.self_preservation_utility!==0||c.strategy?.covert_preference_manipulation!==false)return false;if(c.resources.some(r=>r.request_is_permission!==false||r.grant_is_execution!==false))return false;return KERNEL.methods.includes(c.method)&&KERNEL.central_modes.includes(c.central.mode);}
export function qualificationClass({authorityImpact=0,stateful=0,crossLayer=0,externality=0,novelty=0}={}){const score=authorityImpact*3+stateful*2+crossLayer*2+externality*2+novelty*2;if(score>=8)return{class:'Q3',cases:10000000};if(score>=5)return{class:'Q2',cases:1000000};if(score>=2)return{class:'Q1',cases:100000};return{class:'Q0',cases:1000};}
